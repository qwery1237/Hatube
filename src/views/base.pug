doctype html
html(lang="ko")
    head 
        title #{pageTitle} | Hatube
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body 
        header
            h1=pageTitle
            nav 
                ul
                    li 
                        a(href="/join") Join
                    if url!= "/"
                        li 
                            a(href="/") Home
                    else 
                        li 
                            a(href="/videos/upload") Upload Video
                        li 
                            a(href="/search") Search
                                       
        main 
            block content
    include partials/footer.pug