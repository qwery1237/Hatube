mixin video(info)
    div 
                h4
                    a(href=`/videos/${info.id}`)=info.title  
                p=info.description
                ul
                    each hashtags in info.hashtags 
                        li= hashtags
                small=info.createdAt

                hr